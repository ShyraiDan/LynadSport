/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['img-cdn.hltv.org', 'cdn.sanity.io', 'assets.goal.com', 'cdn.basketballnews.com']
  }
}

export default nextConfig

